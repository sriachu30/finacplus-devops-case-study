# Base URL of the FinacPlus FastAPI service.
# The frontend runs fully on mock/static data if this is unset or unreachable —
# see src/services/api.js for the fallback boundary.
VITE_API_BASE_URL=http://localhost:8000
